datapulse:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DBT_HOST', 'db') }}"
      port: 5432
      user: datapulse
      password: datapulse
      dbname: datapulse
      schema: public
      threads: 4